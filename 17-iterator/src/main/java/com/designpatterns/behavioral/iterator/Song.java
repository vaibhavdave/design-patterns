package com.designpatterns.behavioral.iterator;

/** A single playlist entry. Deliberately trivial — the pattern being demoed is traversal, not data. */
public record Song(String title, String artist) {
}
