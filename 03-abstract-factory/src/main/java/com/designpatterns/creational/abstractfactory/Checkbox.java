package com.designpatterns.creational.abstractfactory;

/** Abstract product #2 of the family — see {@link Button} for why it's paired, not standalone. */
public interface Checkbox {

    String render();
}
